# Simple Memo App

This is a simple serverless memo application built with React and LocalStorage.

## Development Rules
- Use React Hooks for state management.
- Keep all styles in raw CSS files. No CSS framework.
- Build with Vite.
- Port should be 3000.
